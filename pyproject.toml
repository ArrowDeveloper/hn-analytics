[project]
name = "hn-analytics"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "psycopg[binary]",
    "python-dotenv",
]

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]